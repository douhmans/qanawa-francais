# كيف تختبر النموذج الأولي وماذا تتوقّع أن ترى

**أين؟** في لوحة المعاينة على يمينك: بطاقة **«Qanawa — Prototype (élève)»** تفتح النموذج مباشرة.
فضاء الأستاذ من الزرّ أعلى اليمين (أو أضف `teacher.html` إلى الرابط).
لا تثبيت، لا حساب، لا اعتمادات: كل شيء يعمل في المتصفّح.

النموذج مقصود به **تجربة التدفّق البيداغوجي** لا الذكاء الاصطناعي: الصوت من محرّك المتصفّح، والروبوت
بقواعد محلية، والبيانات محفوظة على جهازك وحدك (localStorage). أربع بطاقات جاهزة:
M1 قصيدة *Le boulanger* (نصّ الكتاب ص10، ومؤلفها في **الملك العام** ⇒ نستطيع عرضه قانونيًا)،
وM5 · M6 · M8 نصوص أصلية محاكية للبرنامج.

---

## أ) اختبار التلميذ — 12 خطوة (≈ 12 دقيقة)

| # | افعل هذا | النتيجة المتوقّعة |
|---|---|---|
| 1 | افتح الصفحة | شاشة ترحيب تطلب **اسم الدّلع فقط** + اختيار صورة رمزية + مستوى ذاتي بثلاث بطاقات. **لا** اسم عائلة ولا مدرسة ولا هاتف |
| 2 | اكتب «سلمى» ← «ادخل المنصّة» | تظهر بطاقات النصوص الأربعة مع شريط تقدّم لكل بطاقة ونجوم ★ في الأعلى |
| 3 | اضغط «افتح النصّ» | **الخطوة 1 = الغلاف + التنبّؤ**: سؤالان مع صور. اختر أي إجابة ← تشجيع «تخمين ذكي» **بلا تنقيط** (الفضول لا يُقيَّم بالصواب) |
| 4 | «التالي» → حقيبة الكلمات | لكل كلمة: فرنسية + تقطيع `bou · lan · ger` + نطق بالعربية `بوﻟانْجِي` + ترجمة (وأحيانًا سطر بالدارجة) + emoji + مثال. 3 أزرار: سمعتُها / نطقتُها / حفظتها |
| 5 | اضغط 🔊 عادي ثم 🐢 بطيء | المتصفّح ينطق بالفرنسية، والكلمة تُظلَّل أثناء النطق. إن لم تسمع شيئًا → فقرة «لا صوت» أدناه |
| 6 | «التالي» → قراءة مُرافَقة | النصّ مقسّم جُمَلًا، مع: ▶️ اقرأ الكل، 📏 سطّر سطرًا (سطر واحد معزول على الشاشة)، 🌐 إظهار/إخفاء الترجمة، 🐢 ضبط السرعة، ⭐ «لم أفهمها» |
| 7 | انقر على أي كلمة داخل الجملة | تسمعها + تنفتح بطاقة صغيرة فيها تقطيعها وترجمتها |
| 8 | اضغط ⭐ على جملة | تُعلَّم بنجمة صفراء وستُعاد أولًا عند المراجعة |
| 9 | «التالي» → اقرأ بصوتك | تقرأ الجملة بصوتك ثم تكتب عدد الكلمات الصحيحة ← يحوّلها لـ **WCPM** + نجمة/ورقة/شجرة 🌱🌿🌳، **ولا يعطيك علامة مدرسية**. يختار 3 «كلمات-سرّ» للتدريب عليها |
| 10 | «التالي» → فهم النصّ | 6 عناصر بالترتيب الرسمي: عامّة ← تحليلية ← مفردات ← تجاوز. أجب خطأً عمدا: يعطيك **تلميحًا + رقم الجملة + زر استماع لها**، ولا يعطيك الجواب أبدًا |
| 11 | «التالي» → اللعبة ثم «ما يجب حفظه» | لعبة سماع-واختر بالـ5 كلمات، ثم بطاقات حفظ + عبارة «احكِ على طريقتك»؛ عند النهاية: **قنابل ورق ملوّنة** لمرة واحدة + نجوم |
| 12 | ارجع للرئيسية ← زر **نور** 🌟 | اكتب «أنا متوتر» ← بروتوكول تنفّس 4-4-4 + كلمة واحدة ناجحة + نجمة. اكتب «أعطني الجواب» ← **يرفض بلطف** ويعطي تلميحًا. اكتب «ما اسم عائلتك؟» ← يرفضذكر المعلومات الشخصية |

**تحقّق من التتبّع:** عد للرئيسية ← شريط التقدّم تغيّر، وعدد النجوم زاد، و«🌳 حديقتك: N كلمة» غير صفر.
**تحقّق من سقف اليوم:** واصل اللعب حتى 20 ★ ← تظهر «رصيد اليوم مكتمل — جرّب غدًا 🌱» (حماية من الإفراط).
**تحقّق من المراجعة:** بعد إنهاء بطاقتين تظهر «🔁 مراجعة اليوم» في الرئيسية؛ اضغطها واختر صعبة/مقبولة/سهلة ← تتغيّر مواعيد العودة تلقائيًا.
**تحقّق من العمل دون اتصال:** بعد فتح الصفحة مرّة، أطفأ الإنترنت (أو DevTools → Network → Offline) وأعد التحميل ← التطبيق يشتغل.

---

## ب) اختبار فضاء الأستاذ

من الشريط العلوي اضغط **فضاء الأستاذ** (أو `/teacher.html`). ترَى:

1. **خريطة القسم** 5 مكوّنات × 🌱🌿🌳 + WCPM + نسبة الفهم — **بلا ترتيب ولا مقارنة علنية**.
2. **صندوق أولويّة القسم** يقترح تدخّلًا مبنيا على أرقام حقيقية («لا حصة فونيتيك في التوقيت ← محطة نطق 4 دقائق»).
3. **لكل تلميذ: 3 قوّة + 3 ضعف بمثال فعلي** من إجابته (مثال: `boule/poule`، `pâtisserie ← باتيسري`).
4. **إسناد نصّ + درجة الدعم D1→D4** بزَرّ واحد؛ و«تلميذ هذا المتصفّح» يظهر في القائمة إن جرّبت من نفس الجهاز، بأرقامك الحقيقية من localStorage.
5. **شبكة C1→C7** بمستويات الإتقان `0 / + / ++ / +++` (كما في الدليل ص60–62)، + **تصدير CSV** (ملف مجرّد البيانات) + **🖨️ طباعة/PDF**.
6. **صفّ مراجعة الجودة**: لكل بطاقة فحص آلي (جُمل > 14 كلمة، ترجمة ناقصة، malette، حالة الحقوق).

---

## ج) إن لم تسمع صوتًا فرنسيًا (السبب الأكثر شيوعًا)

- **أندرويد/كروم**: الإعدادات ← البحث «Langu» ← اللغات ← **تحديث بيانات النطق** + تثبيت الصوت الفرنسي `fr-fr`.
- **ويندوز/إيدج**: Settings ← Time & Language ← Language & Region ← Add a language ← **French (France)**، ثم في Edge: *Speech preferences* ← Get online voices.
- **iOS/سفاري**: الإعدادات ← عام ← لوحة المفاتيح ← **إضافة لغة ← Français** (يضيف صوت Siri الفرنسبعض الأحيان صوت المتصفّح يبقى إنجليزيًا؛ لا بأس للتجربة، أمّا التظليل فيعمل).
- الصوت هنا **افتراضيًا من المتصفّح** (مجاني ويعمل دون إنترنت بعد التثبيت). في النسخة النهائية طبقة `SpeechProvider` تضيف Azure/Google بأصوات مدرسية أفضل مع `word boundary` دقيق — وهو ما سيغيّر تظليل karaoke من «تقديري» إلى «مزامَن حرفيّا».

---

## د) ما هو **موثَّق** مقابل ما هو **تجريبي** (حتى لا تختبر الشيء الخطأ)

| البند | حالته الآن |
|---|---|
| التدفّق البيداغوجي (7 خطوات مطابقة لمراحل الدليل ص33-34) | **مطبَّق ويعمل** |
| سياسة «تلميح لا جواب» + رفض طلب الحل | **مطبَّقة ومختبَرة آليًا** (`tools/check_prototype.mjs`: يفحص أن لا hint يحتوي الجواب) |
| الحقيبة/الحديقة/أجندة المراجعة + سقف 20★/يوم | **يعمل** (SM-2 مبسّطة داخل المتصفّح) |
| تظليل الكلمة أثناء النطق | **تقديري** (زمن/أحرف) — يحتاج `word boundary` من مزوّد صوت احترافي |
| قياس النطق | **تقريبي**: كلمات صحيحة/دقيقة فقط؛ لا تقييم صوتيمات |
| روبوت «نور» | **قواعد محلية** (5 أنماط). يحتاج LLM + سجلّ تدقيق + مرشّحات (S6) |
| البطاقات | **4 من 8**؛ المخرَج من `tools/build_official_card.py` على صورة صفحة الكتاب هو الطريق لبقية الـ40 نصًّا (S9) |
| حفظ التقدّم على خادم / حساب أستاذ / رمز PIN للوليّ | **غير موجود بعد** (S0–S1) |

---

## هـ) إعادة الإنتاج والتحقّق من غير متصفّح

```bash
# 1) مخطّط البيانات + سياسة «تلميح لا جواب»:
node tools/check_prototype.mjs
# cartes: 4 · énoncés: 33 · mots: 25 · items: 22 · contrastes: 6 → ✅ conforme au schéma
node -e "for (const f of ['app.js','data.js','teacher.js','sw.js']) new Function(require('fs').readFileSync('prototype/'+f,'utf8')); console.log('syntax OK')"
python3 tools/check_curriculum.py       # ✅ référentiel conforme au programme officiel
# وبعدها يفتح الحarnais (harnais) المسار كلّه آليًا من نفس العنوان:
node tools/harnais_prototype.mjs
# → 40 تحقّقًا (boot، 7 خطوات، malette، قارئ، quiz، نور، سقف النجوم، مراجعة، فونيتيك، فضاء الأستاذ)
# ولسيّر خادم يدويًا:
cd prototype && python3 -m http.server 4173 --bind 0.0.0.0
```
