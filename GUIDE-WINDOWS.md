# نسخة Windows — التثبيت في 4 خطوات

الملف الجاهز: **`Qanawa-windows.zip`** (76,5 ك.ب) في واجهة الملفات — يحتوي كل شيء.

1. **فُكّ الضغط كاملاً** إلى مجلد قابل للكتابة، مثل `C:\Qanawa\` (لا تخرج `Qanawa.exe` وحده).
   البنية المطلوبة:
   ```
   C:\Qanawa\Qanawa.exe
   C:\Qanawa\prototype\index.html
   C:\Qanawa\prototype\app.js …
   C:\Qanawa\README-WINDOWS.md
   ```
2. **نقرة مزدوجة على `Qanawa.exe`** ← يفتح متصفّح على `http://localhost:8137/`.
   لا مثبّت، لا صلاحيات إداري، ولا منفذ مفتوح خارج الجهاز.
3. إذا ظهر «**Windows protected your PC**» ← *More info* → *Run anyway* (برنامج غير موقَّع، لا فيروس).
4. ثبّت الاختصار في شريط المهام (كليك يمين → *Pin to taskbar*) لتفتح المنصّة كل صباح بنقرة.

**التحقّق بنفسك** (سطر الأوامر):
```bat
certutil -hashfile Qanawa-windows.zip SHA256
   → 76cb6229531e79b3d5016827dabeded75ffb6c61df190fcb2016e3f81282c375
C:\Qanawa\Qanawa.exe --serve-only --port 8137
curl http://localhost:8137/health      → ok C:\Qanawa\prototype
curl http://localhost:8137/../../windows/win.ini  → 404  (الوصول خارج المجلد ممنوع)
```

**ماذا يفعل هذا الـ exe؟** يشغّل خادمًا محليًا يقدّم مجلد `prototype` ثم يفتح المتصفّح (Edge ← Chrome ←
المتصفّح الافتراضي)، ويترك أيقونة في علبة النظام فيها *Ouvrir* / *Quitter*. نحتاج خادمًا وليس نقرة على
`index.html` لأنّ `file://` يمنع العمل دون اتصال (service worker) ويجعل حفظ تقدّم التلميذ غير موثوق.

**إعادة بناء الـ exe محليًا** (اختياري، يستعمل `csc.exe` الموجود أصلًا في Windows):
```bat
cd C:\Qanawa\ && win32-src\build_exe.bat   (أو انسخwin32-src إلى مجلد win32 ثم: build_exe.bat)
```
المصادر كاملة ومقروءة: ثلاثة ملفات فقط: `QanawaLauncher.cs` (الخادم + النافذة)، `QanawaIcon.cs` (يرسم الأيقونة)،
`build_exe.bat` (يستدعي `csc.exe`) — الترخيص AGPL-3.0، والـ exe لا يكتب شيئًا خارج `%TEMP%` والمجلد.

**حدود يجب أن تعرفها**:
- **الصوت** فرنسيّ فقط إن كانت حزمة `French (France)` مثبّتة في Windows (إلاّ فنطق إنجليزي أو صمت).
- **قياس القراءة بصوت عالٍ** يستعمل `SpeechRecognition`؛ إن غاب عندك → الحقل اليدوي «عدد الكلمات الصحيحة» يعطي نفس مؤشّر WCPM.
- **التقدّم محليّ للجهاز والمتصفّح**: على كل تلميذ أن يستعمل نفس الجهاز ونفس المتصفّح، وإلاّ يبدأ من الصفر
  (المزامنة عبر حسابات = السبرنتان S0–S1). فضاء الأستاذ يُصدّر CSV تنقله على مفتاح USB.
- **حذف البرنامج** = حذف مجلد `C:\Qanawa` (لا شيء كُتب في السجلّ).

## «الـ exe يفتح صفحة بيضاء»

ثلاث أسباب ممكنة، والنسخة الحديثة تفرّق بينها وحدها:

| السبب | ما تراه | الحل |
|---|---|---|
| تشغيل الملف من داخل الـ ZIP دون استخراجه (الأكثر شيوعًا) | صفحة «الملف غير موجود» + نافذة «démarrage incomplet» | كليك يمين على الـ ZIP ← **Extract All / استخراج الكل** ثم شغّل `Qanawa.exe` من المجلّد الجديد |
| فتح المتصفّح قبل جاهزية الخادم | صفحة بيضاء ثم «تعذّر الوصول» | مُصحَّح: البرنامج ينتظر اشتغال المنفذ (٤ ثوانٍ) قبل فتح المتصفّح، وزرّ **Ouvrir** في نافذة البرنامج يُعيد المحاولة |
| المنفذ 8137 محجوز أو ممنوع | نافذة خطأ لا تُغلق نفسها | `Qanawa.exe --port 8642` أو: `cd prototype` ثم `python -m http.server 8137` |

تشخيص في سطرين (من داخل المجلّد المستخرَج):
```bat
Qanawa.exe --serve-only --port 8137
curl http://localhost:8137/health
    ok C:\Qanawa\prototype · 9 fichiers     ← كل شيء سليم، افتح http://localhost:8137/
    ok ...\prototype · dossier absent        ← مجلد prototype ليس بجانب البرنامج
```
النسخة الحالية: لا تُغلق نوافذ الخطأ تلقائيًا، وتعطي صفحة تشخيص بدل شاشة بيضاء.

## «لا أستطيع كتابة اسمي» عند فتح `index.html` مباشرة

هذا سلوك Chrome وEdge مع الملفات المفتوحة بـ `file://`: **يرفضان التخزين المحلي** فلا يُحفظ الاسم.
الحلّ الأسهل: **شغّل `Qanawa.exe`** (يفتح على `http://localhost:8137/` والتخزين يعمل).
وإلى أن تفعل ذلك، النسخة الحالية تتصرّف هكذا:
- الحقل يأخذ التركيز تلقائيًا، والكتابة فيه تعمل (عربيًّا أو لاتينيًّا) — ثم اضغط **Enter** للدخول؛
- إذا تركته فارغًا يُدخل «قارئ» بدل أن يتعطّل الزرّ؛
- شريط أصفر في الأعلى يقول «⚠️ هذا الجهاز لا يحفظ التقدّم الآن» مع زرّ *كيف أُصلح ذلك؟* يعرض الأوامر؛
- الدروس والألعاب والصوت كلّها تعمل، غير أنّ النجوم والحديقة تُفقد عند إعادة التحميل.

تفاصيل أكثر (الطباعة، تقليل الحركات، الشبكة النارية، الأسئلة الشائعة) في `README-WINDOWS.md` داخل الـ ZIP،
وفحص آليّ لهذه الحالة: `node tools/test_file_mode.mjs`.
